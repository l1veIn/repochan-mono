---
import MorphPage from "../components/MorphPage.astro";
import { getContent, site } from "../lib/site";

const repoUrl = site.project.repositoryUrl ?? "/";
---

<MorphPage content={getContent("zh")} locale="zh" repoUrl={repoUrl} />

{
  "name": "@repochan/cli-pi-base",
  "version": "0.1.0",
  "private": true,
  "description": "[DEPRECATED/ARCHIVE] Old RepoChan CLI that embedded the Pi runtime (TUI wizard, agent sessions). Kept for reference; the live CLI is packages/cli. Not published.",
  "type": "module",
  "license": "MIT",
  "author": "l1veIn",
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.1",
    "@earendil-works/pi-tui": "^0.79.6",
    "@juicesharp/rpiv-ask-user-question": "^1.20.0",
    "@repochan/core": "workspace:*",
    "@repochan/image-gen-pi": "workspace:*",
    "chalk": "^5.6.2",
    "repochan-pi": "workspace:*",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0",
    "vitest": "^2.1.9"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
